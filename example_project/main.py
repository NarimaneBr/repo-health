from .utils import helper

def run():
    print("Running example project")
    helper()

def very_complex_function():
    a = 1
    if a:
        if a:
            if a:
                if a:
                    if a:
                        if a:
                            if a:
                                if a:
                                    if a:
                                        if a:
                                            if a:
                                                pass
